// tests/board.test.ts
import { Board } from '../src/game/board';

describe('Board', () => {
  let board: Board;

  beforeEach(() => {
    board = new Board();
  });

  test('should initialize with 24 pieces', () => {
    // Verifica se o tabuleiro foi criado corretamente
  });

  test('should allow valid moves', () => {
    // Testa movimentos válidos
  });
});